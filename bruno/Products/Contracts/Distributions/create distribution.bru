meta {
  name: create distribution
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/products/2/contracts/2/distributions
  body: json
  auth: inherit
}

body:json {
  {
    "type": "A",
    "access_service_id": 1
  }
}

script:pre-request {
  const getToken = require("./get-token");

  const token = await getToken(["scope_tn1"]);

  req.setHeader("Authorization", `Bearer ${token}`);

}

settings {
  encodeUrl: true
}
