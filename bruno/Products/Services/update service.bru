meta {
  name: update service
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/products/4/services/4
  body: json
  auth: inherit
}

body:json {
  {
    "type": "WFS"
  }
}

script:pre-request {
  const getToken = require("./get-token");

  const token = await getToken(["scope_tn1"]);

  req.setHeader("Authorization", `Bearer ${token}`);

}

settings {
  encodeUrl: true
}
