meta {
  name: update team by team member
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/teams/1
  body: json
  auth: inherit
}

body:json {
  {
    "po_name": "Someone Else"
  }
}

script:pre-request {
  const getToken = require("./get-token");

  const token = await getToken(["scope_tn1"]);

  req.setHeader("Authorization", `Bearer ${token}`);

}

settings {
  encodeUrl: true
}
