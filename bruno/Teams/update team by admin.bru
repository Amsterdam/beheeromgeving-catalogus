meta {
  name: update team by admin
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/teams/1
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Admin can update name"
  }
}

script:pre-request {
  const getToken = require("./get-token");

  const token = await getToken(["admin"]);

  req.setHeader("Authorization", `Bearer ${token}`);

}

settings {
  encodeUrl: true
}
